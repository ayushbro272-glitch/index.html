<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Free Fire Diamond APK Download</title>
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #1f1c2c, #928dab);
            color: #ffffff;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        .container {
            width: 100%;
            max-width: 450px;
            background: rgba(0, 0, 0, 0.75);
            border-radius: 20px;
            padding: 30px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
            text-align: center;
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        header h1 {
            font-size: 22px;
            margin-bottom: 10px;
        }

        .app-card {
            margin: 25px 0;
        }

        .image-box {
            width: 120px;
            height: 120px;
            margin: 0 auto 20px auto;
            border-radius: 25px;
            overflow: hidden;
            border: 3px solid #ffcc00;
            box-shadow: 0 0 15px rgba(255, 204, 0, 0.5);
        }

        .app-img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .info-box {
            margin-bottom: 25px;
        }

        .highlight-en {
            font-size: 16px;
            font-weight: 600;
            color: #ffcc00;
        }

        /* Full width green button style */
        .download-btn {
            display: block;
            width: 100%;
            background: linear-gradient(135deg, #11998e, #38ef7d);
            color: white;
            text-decoration: none;
            padding: 16px 20px;
            border-radius: 12px;
            font-weight: 700;
            font-size: 18px;
            text-align: center;
            box-shadow: 0 5px 15px rgba(56, 239, 125, 0.4);
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }

        .download-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(56, 239, 125, 0.6);
        }

        footer {
            font-size: 12px;
            color: #aaa;
            margin-top: 15px;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            padding-top: 15px;
        }
    </style>
</head>
<body>

    <div class="container">
        <!-- Header / Title Section -->
        <header>
            <h1>Free Fire Diamond Generator</h1>
        </header>

        <!-- App Preview / Image Section -->
        <div class="app-card">
            <div class="image-box">
                <!-- Yahan apni app ki logo image ka naam dalein (jaise app-logo.png) -->
                <img src="app-logo.png" alt="App APK Preview" class="app-img">
            </div>
            
            <div class="info-box">
                <p class="highlight-en">Download App & Get Free Diamonds!</p>
            </div>

            <!-- DOWNLOAD APK BUTTON WITH YOUR DIRECT DRIVE LINK -->
            <a href="https://drive.google.com/uc?export=download&id=16xaD0iWJM5A9PZ6-nheVj2usnbYbNhpz" class="download-btn">
                DOWNLOAD APK
            </a>
        </div>

        <!-- Footer Instructions -->
        <footer>
            <p>Safe & Secure Download. Install the app to claim your rewards.</p>
        </footer>
    </div>

</body>
</html>
